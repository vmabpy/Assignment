import React from 'react'
import { View, Text, StyleSheet } from 'react-native'
import { padding } from '../../globals/constants'

const ImageCenterButton = (props) => {
    return (
        <View>

        </View>
    )
}

const styles = StyleSheet.create({

})