import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Setting = (props) => {
  return (
    <View style={styles.container}>
      <Text>Setting is comming soon</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default Setting;
